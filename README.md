
  # Start project with files

  This is a code bundle for Start project with files. The original project is available at https://www.figma.com/design/fQNc7pA0TbPQi5A93Jqend/Start-project-with-files.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  